# AGENTS

## HTML
- Always write semantic HTML. Prefer elements like `section`, `header`, `nav`, `main`, `article`, `figure`, `blockquote`, `button`, and `label` when they better describe structure or intent. Avoid generic `div`/`span` when a semantic element applies.
